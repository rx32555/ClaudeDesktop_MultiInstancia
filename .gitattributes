# Auto detect text files and perform LF normalization
* text=auto

# Scripts de Windows deben conservar CRLF para compatibilidad con cmd/powershell
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
